<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <!-- ========== APPENDERS ========== -->
    <Appenders>

        <!-- Console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- File con rotazione e retention -->
        <RollingFile
                name="RollingFile"
                fileName="${catalina.base}/logs/chronogram.log"
                filePattern="${catalina.base}/logs/chronogram-%d{yyyy-MM-dd}-%i.log">

            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>

            <!-- Rotazione per data e dimensione -->
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>

            <!-- Rollover max 10 file + auto-delete > 7 days -->
            <DefaultRolloverStrategy max="10" fileIndex="min">
                <Delete basePath="${catalina.base}/logs" maxDepth="1">
                    <IfFileName glob="chronogram-*.log"/>
                    <IfLastModified age="7d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

    </Appenders>

    <!-- ========== LOGGERS ========== -->
    <Loggers>

        <!-- Logger di progetto -->
        <Logger name="it.unicas" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

        <!-- Root (INFO) -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>

    </Loggers>
</Configuration>
